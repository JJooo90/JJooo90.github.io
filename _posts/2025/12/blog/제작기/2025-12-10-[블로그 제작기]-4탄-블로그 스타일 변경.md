---
title: "[blog] 블로그 제작기 4탄 - 블로그 스타일 변경"
categories: [blog, 제작기]
tags: [blog, github]
toc: false
---
### 목표
1. Home 페이지 → Zapier-style “문서 인덱스 + 섹션형 홈”
2. 프로젝트 Overview 페이지 템플릿 → Zapier 문서 구조 기반
3. 일반 글 템플릿 → Zapier 문서 스타일 기반

| https://zapier.com/engineering/great-documentation-examples/


### 🧭 전체 구성 과정 요약
✔ Step 1 — 홈 화면을 Zapier-style 문서 홈으로 바꾸기
→ _layouts/home.html 새로 생성

✔ Step 2 — 프로젝트 Overview 템플릿 만들기
→ _posts/project/0000-00-00-overview.md 또는 /project/index.md

✔ Step 3 — 일반 글 템플릿 생성
→ Zapier-style 기술 문서 포맷

✔ Step 4 — 디자인 개선 (선택)
→ _sass/custom.scss 커스터마이징


---
### Step 1 : Zapier 스타일 홈 화면 적용
(Chirpy 전용)

#### ✔ 목적
기존 최신 글 나열 방식 → 카테고리 섹션 + 문서 인덱스 중심으로 변경
Zapier 문서처럼 깔끔하게 정리된 홈 구성 만들기.

1. 새 파일 생성
경로 : /_layouts/home.html (파일이 없으면 새로 만들어도 됨)

2. Zapier-style Home Layout 코드 (복붙)

```html
---
layout: default
---

<div class="zap-home">

  <h1 class="zap-title">📘 JJooo 기술 블로그</h1>
  <p class="zap-desc">정리된 기술 문서, 프로젝트 기록, 그리고 개발 여정을 담은 공간</p>

  <!-- 최신 글 3개 -->
  <section>
    <h2>🆕 최근 업데이트</h2>
    <ul class="zap-post-list">
      {% for post in site.posts limit:3 %}
      <li>
        <a href="{{ post.url | relative_url }}">{{ post.title }}</a>
        <span class="date">{{ post.date | date: "%Y-%m-%d" }}</span>
      </li>
      {% endfor %}
    </ul>
  </section>

  <!-- 카테고리별 섹션 -->
  <section>
    <h2>📚 블로그 제작기</h2>
    {% assign blogs = site.categories.blog | slice: 0, 3 %}
    <ul class="zap-post-list">
      {% for post in blogs %}
      <li><a href="{{ post.url | relative_url }}">{{ post.title }}</a></li>
      {% endfor %}
    </ul>
  </section>

  <section>
    <h2>🧪 AI / 컴퓨터 비전</h2>
    {% assign ai_posts = site.categories.ai | slice: 0, 3 %}
    <ul class="zap-post-list">
      {% for post in ai_posts %}
      <li><a href="{{ post.url | relative_url }}">{{ post.title }}</a></li>
      {% endfor %}
    </ul>
  </section>

  <section>
    <h2>🛠 Dev / 개발</h2>
    {% assign dev_posts = site.categories.dev | slice: 0, 3 %}
    <ul class="zap-post-list">
      {% for post in dev_posts %}
      <li><a href="{{ post.url | relative_url }}">{{ post.title }}</a></li>
      {% endfor %}
    </ul>
  </section>

  <section>
    <h2>🚀 프로젝트 문서</h2>
    {% assign project_posts = site.categories.project | slice: 0, 3 %}
    <ul class="zap-post-list">
      {% for post in project_posts %}
      <li><a href="{{ post.url | relative_url }}">{{ post.title }}</a></li>
      {% endfor %}
    </ul>
  </section>

  <section>
    <h2>💥 문제 해결 / TroubleShooting</h2>
    {% assign trouble_posts = site.categories.trouble | slice: 0, 3 %}
    <ul class="zap-post-list">
      {% for post in trouble_posts %}
      <li><a href="{{ post.url | relative_url }}">{{ post.title }}</a></li>
      {% endfor %}
    </ul>
  </section>

</div>

```

| 기존 페이지 | 변경 후 페이지 |
|------|------|\
| ![기존 페이지]({{ "/assets/img/posts/2025/12/blog/2025-12-10/img_01.png" | relative_url }}) | ![변경 후 페이지]({{ "/assets/img/posts/2025/12/blog/2025-12-10/img_02.png" | relative_url }}) |

